import { FolderKanban, Handshake, House, Link2, MonitorSmartphone, Palette, UserRound, Wrench } from "lucide-react";
import type { LucideIcon } from "lucide-react";

export const navigationIcons: readonly LucideIcon[] = [
  House,
  UserRound,
  FolderKanban,
  MonitorSmartphone,
  Handshake,
  Palette,
  Wrench,
  Link2,
];

export const fallbackNavigationIcon = Link2;
